# === Node.js / MERN ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn/
dist/
build/

# Environment variables (NEVER commit secrets/API keys/DB URIs)
.env
.env.local
.env.development
.env.production
.env.*.local

# === Python / ML ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
pip-log.txt
pip-delete-this-directory.txt

# Jupyter
.ipynb_checkpoints/

# === ML artifacts (large / regenerable — don't bloat the repo) ===
# Keep the trained model out UNLESS you specifically want to showcase it;
# most recruiters care about code, not binary blobs.
models/*.pkl
models/*.joblib
models/*.txt
!models/metrics.json
data/*.csv
ml-service/data/raw/*.csv
!data/sample_transactions.csv

# === Databases ===
*.sqlite3
*.db

# === Docker ===
*.log
docker-compose.override.yml

# === OS / Editor junk ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# === Misc ===
*.tmp
*.bak
coverage/
.eslintcache